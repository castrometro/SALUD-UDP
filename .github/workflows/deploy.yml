name: Deploy to Production (deshabilitado)

on:
  workflow_dispatch:

jobs:
  deploy:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Workflow deshabilitado
        run: echo "El despliegue automático por runner self-hosted fue deshabilitado."
